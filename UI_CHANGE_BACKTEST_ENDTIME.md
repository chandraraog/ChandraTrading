# Backtest End Time

Added `End Time (Optional)` to the Timing Candle backtest form.
Blank means the backtest uses the full requested session/data range.
A supplied time limits the backtest to that configured time.
